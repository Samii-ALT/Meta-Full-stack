var Num = Math.random(); // generates random decimal from 0.0 to 0.99

// Prints Random decimal from 0.00 to 0.99
console.log(Num);

// Prints Random value from 0 to 9.99
console.log(Num*10);   // 0 is only when math.random generates 0.0 but it is very rare case.
