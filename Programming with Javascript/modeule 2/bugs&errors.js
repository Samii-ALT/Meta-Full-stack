function add(a, b){
    console.log(a+b);
}
add("1", "2");   // This is a bug bcz program is still running but output is 12.


console.log(c+d);  // Reference error c and d is not defined. Program will not run.

var greet = "Hello; 
// Syntax error bcz the " is not closed.

5.pop() // This is Type error because there is no pop method for numbers.
