// Encapsulation means that hide what the actual function looks from inside.

// For example..
console.log("abc".toUpperCase());

// Java process "toUpperCase" function internally but we don't know how 
// and we don't need to know we just use it.
// similarly encapsulation mean's hiding things don't need to know.